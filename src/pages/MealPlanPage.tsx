import { ComingSoon } from "../components/ComingSoon";
import { Lang } from "../lib/i18n";

// Replaced with the real meal-plan generator in Phase 6.
export function MealPlanPage({ lang }: { lang: Lang }) {
  return <ComingSoon lang={lang} icon="🍽️" />;
}
